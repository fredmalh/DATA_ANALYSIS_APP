# Build Configuration Files

This directory contains configuration files for building executables.

## Files

- `pyinstaller.spec` - PyInstaller specification file for building the executable

## Usage

These files are used automatically by the build scripts in the `scripts/` directory.
You typically don't need to edit them manually unless you need to customize the build process.

